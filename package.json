{
  "name": "connectify",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm --prefix forntend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "install:all": "npm install --prefix backend && npm install --prefix forntend",
    "build": "npm install --prefix backend && npm install --prefix forntend && npm run build --prefix forntend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
